# Brock Session Log — 2026-03-16T19:00Z

**Agent:** Brock (Web Engine Dev)  
**Mode:** Background (completed)  
**Duration:** Session completion  

## Summary
Fixed 3 critical rendering and input bugs affecting HiDPI displays and boot sequence.

## Bugs Fixed
1. **BUG-002 (P0):** HiDPI canvas resolution
   - Added `resolution: window.devicePixelRatio || 1` to PixiJS app.init()
   - Added `autoDensity: true` to enable automatic CSS scaling
   - No downstream code changes needed — app.screen dimensions already in CSS pixels
   - **Result:** Game renders crisp on all device pixel ratios (Retina, 1.5x, etc.)

2. **BUG-009 (P1):** BootScene wait for input
   - Replaced auto-transition in BootScene with input gating
   - Loading bar completes → "Press any key" text pulses → waits for keyboard/click/touch
   - Uses `transitioning` flag (convention from PR #281) to prevent duplicate async transitions
   - **Result:** Players have time to read boot message before entering menu

3. **BUG-006 (P1):** EventBus listener cleanup in GardenScene
   - Introduced `listenTo()` helper function in GardenScene
   - Wraps `eventBus.on()` and tracks cleanup closures
   - All 23 EventBus subscriptions now route through this helper
   - `destroy()` iterates cleanup array and calls `eventBus.off()` with correct function references
   - **Result:** Memory leak eliminated; listeners properly cleaned on scene re-entry

## Build Status
✅ Clean build: `npm run build`

## Convention Established
**All scenes with EventBus subscriptions must use `listenTo()` or equivalent cleanup tracking.** Raw `eventBus.on()` without corresponding `off()` in `destroy()` is a memory leak.

## Decision Added
- **Decision:** Rendering & Input Fixes (BUG-002, BUG-009, BUG-006)
  - Documented HiDPI resolution pattern
  - Documented input gating convention
  - Documented EventBus cleanup pattern for future use
